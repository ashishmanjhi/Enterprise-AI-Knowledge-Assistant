# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# LangChain Ecosystem
langgraph==0.1.19

langchain==0.1.0
langchain-community==0.0.20
langchain-core==0.1.21

# LLM Providers (local)
ollama==0.1.6
# Cloud LLM providers (Phase 10 — install when API keys are configured)
# openai is already pinned below; others are unpinned so pip resolves latest compatible
anthropic>=0.20.0
google-generativeai>=0.5.0
openai==1.10.0
transformers==4.36.2
torch==2.1.2
accelerate==0.25.0
sentencepiece==0.1.99

# Document Processing (Phase 1)
PyPDF2==3.0.1
python-docx==1.1.0

# Enhanced PDF Extraction (Phase 13)
pdfplumber>=0.10.0          # table-aware PDF extraction
# Optional OCR dependencies — install manually when needed:
# pip install pytesseract Pillow pdf2image
# Also requires Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki

# Embeddings & Vector Store (Phase 1)
sentence-transformers==2.3.1
faiss-cpu==1.7.4

# BM25 & Hybrid Retrieval (Phase 2)
rank-bm25==0.2.2

# Database
psycopg2-binary==2.9.9
asyncpg==0.29.0
redis==5.0.1
sqlalchemy==2.0.25
alembic==1.13.1

# Frontend
streamlit==1.30.0
requests==2.31.0

# Utilities
python-multipart==0.0.6
aiofiles==23.2.1

# Reranking (Phase 4)
sentence-transformers==2.3.1

# Evaluation / RAGAS (Phase 5)
ragas==0.2.15
langchain-openai==0.1.3
pandas>=2.0.0

# Auth (Phase 10)
PyJWT>=2.8.0

# Rate Limiting (Phase 15)
slowapi==0.1.9

# Development
pytest==7.4.3
pytest-asyncio==0.23.3
pytest-cov==4.1.0
black==23.12.1
ruff==0.1.9
httpx==0.25.2